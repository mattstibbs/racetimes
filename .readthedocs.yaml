# Read the Docs build configuration for the user manual. See
# docs/deploying-manual.md for setting up the project on Read the Docs.
version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.11"

mkdocs:
  configuration: mkdocs.yml

python:
  install:
    - requirements: requirements-docs.txt
